# docs/

Design and reference material for the AI RFQ project.

| File | What it is |
|---|---|
| `09_price_estimation_architecture.md` | **Governing spec** for the price engine (your superior's doc). *Add the original here* — the code in `src/pricing/` implements its POC slice. |
| `PRICING_ENGINE_DESIGN.md` | How `src/pricing/` maps onto the spec — module-by-module, plus the design decisions and where each doc phase lives. |
| `schema.pdf` | The `ai_rfq_sample` database schema reference (37 tables). *Add the original `schema (1).pdf` here.* |

> The two source documents (`09 - …md` and `schema (1).pdf`) were provided out-of-band and are
> not reproduced here verbatim. Drop the originals into this folder before pushing so the repo
> is self-contained.
